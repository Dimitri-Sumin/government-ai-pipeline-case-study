Current stage: dataset anonymization + normalization in progress (~1000 leads).
Next milestone: first clean schema + initial SQL exploration.
Note: all entities are anonymized, no client names included.
